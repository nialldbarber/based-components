## Usage

### Header

```jsx
import {BasedProvider, Header} from 'based-components';

/**
 * Place a `BasedProvider` at the root of your React application
 */
<BasedProvider>
  <>
    <Header>Default</Header>
  </>
</BasedProvider>;
```
